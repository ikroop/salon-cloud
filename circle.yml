machine:
  node:
    version: 5.5.0
dependencies:
  override:
    - npm install -g typescript   
    - npm install -g mocha
    - cd SalonCloud.Server && npm install
    - cd SalonCloud.Server && tsc
test:
  post:
    - cd SalonCloud.Server && mocha Spec/Authentication.spec.js


